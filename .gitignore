node_modules/

# AgentMinds local key file (auto-generated by connect skill).
# Contains API credentials — must never be committed.
.agentminds.json

# Per-cwd usage tracking written by the local rate limiter.
.agentminds_usage.json
